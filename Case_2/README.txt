Para ejecutar:
	"nmake -f make.mk" 

**

Use the headers and .cpp file "Main" to 
compilate and generate the "a.exe" file

When the file "a.exe" is executed, remove it
and remove the pre-compiled files (".gch")

**